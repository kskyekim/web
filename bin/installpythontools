#!/bin/bash

set -Eeuo pipefail
set -x

# this is to run the python virtual environment commands
sudo apt-get update
sudo apt-get install python3 python3-pip python3-venv python3-wheel python3-setuptools

pip install -r requirements.txt